export default function initUsersController(db) {
  // const allRoomsByUser = async (req, res) => {
  //   const {userId} = req.params;
  //   // create a find query which searches the db for rooms where the username is
  //   const allRooms = db.Room.find({username: userId});
  // };
  // return {
  //   allRoomsByUser,
  // };
}
